import { StrategyCard } from '@/components/strategies/strategy-card';
import { DataTable, PageIntro, Panel, StatusBadge } from '@/components/ui';
import { strategyRuntime } from '@/data/mock-data';
const tone={INHERITED_PROTOTYPE:'neutral',TEST_DEFAULT:'warning',RESEARCH_HYPOTHESIS:'ai'} as const;
export function StrategiesScreen(){return <><PageIntro section="strategies" title="EMA-CROSS-001 governance profile" description="Runtime-shaped mock telemetry for the first paper-cleared strategy." aside={<StatusBadge tone="danger">LIVE CAPITAL $0</StatusBadge>}/><StrategyCard strategy={strategyRuntime}/><div className="strategy-detail-grid"><Panel title="Parameter provenance" subtitle="Source labels are not validation claims"><DataTable headers={['Parameter','Value','Provenance','Interpretation']} rows={strategyRuntime.parameters.map(p=>[<b key={`name-${p.name}`}>{p.name}</b>,p.value,<StatusBadge key={`source-${p.name}`} tone={tone[p.provenance]}>{p.provenance}</StatusBadge>,p.provenance==='INHERITED_PROTOTYPE'?'Inherited from prototype behavior':p.provenance==='TEST_DEFAULT'?'Convenient test value only':'Unvalidated research assumption'])}/></Panel><Panel title="Clearance boundary" subtitle="Independent from autonomy and capital"><div className="clearance-card"><StatusBadge tone="ai">{strategyRuntime.clearance}</StatusBadge><h3>{strategyRuntime.id}</h3><p>Authorized for simulated execution with $100 paper capital. This clearance does not permit live orders.</p><dl><dt>Instruments</dt><dd>{strategyRuntime.instruments.join(' / ')}</dd><dt>Version</dt><dd>{strategyRuntime.version}</dd><dt>Live execution</dt><dd>BLOCKED</dd></dl></div></Panel></div></>}
