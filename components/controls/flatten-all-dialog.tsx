'use client';
import { OctagonX, X } from 'lucide-react';
import { useUIStore } from '@/stores/ui-store';
export function FlattenAllDialog(){const {dialog,closeDialog}=useUIStore();if(dialog!=='flatten')return null;return <div className="dialog-backdrop"><section className="confirm-dialog flatten-dialog" role="alertdialog" aria-modal="true" aria-labelledby="flatten-title"><button className="dialog-close" onClick={closeDialog} aria-label="Close"><X size={17}/></button><span className="danger-symbol"><OctagonX size={22}/></span><p className="section-kicker danger">EMERGENCY LIQUIDATION</p><h2 id="flatten-title">Flatten all paper inventory?</h2><p>Future behavior will cancel pending orders, liquidate active trading inventory, move the execution engine toward 100% cash, and enter emergency halt.</p><label><input type="checkbox"/> I understand FLATTEN ALL is distinct from HALT TRADING.</label><div className="dialog-actions"><button className="secondary-button" onClick={closeDialog}>Cancel</button><button className="danger-button" onClick={closeDialog}>Confirm flatten all</button></div><small className="mock-note">Sprint 1 UI only · no broker action will occur</small></section></div>}
