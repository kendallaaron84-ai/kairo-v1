import { Activity, Clock3, FlaskConical, Gauge, Radio } from 'lucide-react';
import { StatusBadge } from '@/components/ui';
import type { StrategyRuntime } from '@/lib/domain';
const money=(value:number)=>`$${value.toFixed(2)}`;
export function StrategyCard({strategy}:{strategy:StrategyRuntime}){return <article className="canonical-strategy"><div className="canonical-strategy-head"><div><p className="section-kicker">CANONICAL STRATEGY 001</p><h3>{strategy.id} <span>v{strategy.version}</span></h3><p>{strategy.name}</p></div><StatusBadge tone="ai">{strategy.clearance}</StatusBadge></div><div className="runtime-grid"><div><Gauge size={15}/><span>EMA Period</span><b>{strategy.emaPeriod}</b></div><div><Clock3 size={15}/><span>Bar Interval</span><b>{strategy.barInterval}</b></div><div><Radio size={15}/><span>Quote Poll</span><b>{strategy.quotePoll}</b></div><div><Activity size={15}/><span>Warmup</span><b>{strategy.warmup.current} / {strategy.warmup.target} bars</b></div><div><FlaskConical size={15}/><span>Latest Signal</span><b>{strategy.latestSignal}</b></div><div><span className="loss-count">0</span><span>Loss Streak</span><b>{strategy.lossStreak}</b></div></div><div className="capital-split"><div><span>Live Capital</span><strong>{money(strategy.liveCapital)}</strong><small>Not authorized</small></div><div><span>Paper Capital</span><strong>{money(strategy.paperCapital)}</strong><small>Governance authorized</small></div></div></article>}
