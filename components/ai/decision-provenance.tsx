import { BrainCircuit, ChartSpline, Check, ScanSearch, ShieldCheck, Zap } from 'lucide-react';
import type { AIDecisionTrace } from '@/lib/domain';
import { Panel, StatusBadge } from '@/components/ui';
export function DecisionProvenance({trace}:{trace:AIDecisionTrace}){const stages=[[ChartSpline,'STRATEGY TRIGGER',trace.trigger],[ScanSearch,'CONTEXT ANALYSIS',trace.context],[BrainCircuit,'AI VERDICT',trace.verdict],[ShieldCheck,'RISK AUTHORIZATION',trace.riskGovernor],[Zap,'EXECUTION ENGINE',trace.execution]] as const;return <Panel title="Decision provenance" subtitle={`Trace ${trace.id}`} action={<StatusBadge tone="ai">AI {trace.authority}</StatusBadge>}><div className="provenance-chain">{stages.map(([Icon,label,value],i)=><div className={`provenance-step p${i}`} key={label}><span><Icon size={17}/></span><small>{label}</small><strong>{value}</strong>{i<stages.length-1&&<i>↓</i>}</div>)}</div><div className="risk-invariant"><ShieldCheck size={15}/><strong>AI CAN NEVER BYPASS OR OVERRIDE THE RISK GOVERNOR.</strong><Check size={14}/></div></Panel>}
