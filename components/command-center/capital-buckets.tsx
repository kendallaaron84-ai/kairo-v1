import { Panel } from '@/components/ui';
import type { CapitalBuilderSnapshot } from '@/lib/domain';
export function CapitalBuckets({data}:{data:CapitalBuilderSnapshot}){const buckets=[['Seed Capital',data.seedReference,'Productive reference','seed'],['Safety Reserve',data.safetyReserve,'Engine unavailable','reserve'],['Ownership Treasuries',data.ownershipTreasuries,'Long-term ownership','ownership'],['Replication Fund',data.replicationFund,'Future cell capital','replication']];return <Panel title="Capital buckets" subtitle="Purpose-separated capital"><div className="bucket-list">{buckets.map(([name,value,detail,tone])=><div className="bucket-row" key={name}><span className={`bucket-mark ${tone}`}/><div><strong>{name}</strong><small>{detail}</small></div><b>${Number(value).toFixed(2)}</b></div>)}</div></Panel>}
