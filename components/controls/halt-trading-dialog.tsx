'use client';
import { AlertTriangle, X } from 'lucide-react';
import { useUIStore } from '@/stores/ui-store';
export function HaltTradingDialog(){const {dialog,closeDialog}=useUIStore();if(dialog!=='halt')return null;return <div className="dialog-backdrop"><section className="confirm-dialog halt-dialog" role="alertdialog" aria-modal="true" aria-labelledby="halt-title"><button className="dialog-close" onClick={closeDialog} aria-label="Close"><X size={17}/></button><span className="warning-symbol"><AlertTriangle size={22}/></span><p className="section-kicker warning">SOFT OPERATIONAL STOP</p><h2 id="halt-title">Halt new trading activity?</h2><p>Future behavior will block new entries, cancel pending entry orders, and disable strategy polling. Existing positions remain under their defined management rules.</p><ul><li>Block new entries</li><li>Cancel pending entry orders</li><li>Leave active positions managed</li></ul><div className="dialog-actions"><button className="secondary-button" onClick={closeDialog}>Keep running</button><button className="warning-button" onClick={closeDialog}>Confirm halt trading</button></div><small className="mock-note">Sprint 1 UI only · no broker action will occur</small></section></div>}
