import { Check, Circle, LockKeyhole, ShieldX } from 'lucide-react';
import type { GovernanceState, MilestoneState } from '@/lib/domain';
import { Panel, StatusBadge } from '@/components/ui';
export function MilestoneLadder({milestones,governance}:{milestones:MilestoneState[];governance:GovernanceState}){return <Panel title="M0–M6 governance ladder" subtitle="Launch and trust progression" action={<StatusBadge tone="warning">SCALE {governance.capitalScale}</StatusBadge>}><div className="milestone-ladder">{milestones.map(m=><div className={`milestone ${m.status}`} key={m.id}><span className="milestone-icon">{m.status==='complete'?<Check size={13}/>:m.status==='active'?<Circle size={11}/>:m.status==='failed'?<ShieldX size={13}/>:<LockKeyhole size={12}/>}</span><b>{m.id}</b><div><strong>{m.label}</strong>{m.progress&&<small>{m.progress.current} / {m.progress.target} fidelity sessions</small>}</div><em>{m.status}</em></div>)}</div><div className="unlock-row"><span>Current Milestone<b>{governance.currentMilestone}</b></span><span>Next Operational Unlock<b>{governance.nextUnlock}</b></span></div></Panel>}
