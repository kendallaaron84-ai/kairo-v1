import { BrainCircuit, KeyRound, ShieldCheck } from 'lucide-react';
import type { GovernanceState } from '@/lib/domain';
import { StatusBadge } from '@/components/ui';
export function AutonomyCard({governance}:{governance:GovernanceState}){const axes=[[BrainCircuit,'AUTONOMY',`Level ${governance.autonomyLevel} — ${governance.autonomyLabel}`,'Agent operating latitude'],[KeyRound,'AUTHORIZED CAPITAL',`$${governance.authorizedCapital.toFixed(2)}`,'Independent capital ceiling'],[ShieldCheck,'STRATEGY CLEARANCE',`${governance.strategyId} — PAPER ONLY`,'Independent strategy permission']] as const;return <article className="governance-card"><div className="governance-title"><div><p className="panel-title">Tri-Axis Governance</p><span>Permissions change independently</span></div><StatusBadge tone="warning">CAPITAL {governance.capitalScale}</StatusBadge></div><div className="governance-axes">{axes.map(([Icon,label,value,detail])=><div key={label}><span className="axis-icon"><Icon size={16}/></span><small>{label}</small><strong>{value}</strong><p>{detail}</p></div>)}</div><p className="governance-invariant">Autonomy ≠ Capital Authorization ≠ Strategy Clearance</p></article>}
